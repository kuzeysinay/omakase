rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tüm koleksiyonlara sadece giriş yapmış kullanıcıların okuma/yazma yapmasına izin ver
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
